# 内部数据分析工具 - 项目说明书

## 一、项目概述

本项目是面向公司内部员工的智能数据分析平台，集成 RAG 知识检索、LLM 智能问答、数据报表分析三大核心能力。系统对数据私密性有严格要求，采用 RBAC 权限控制，支持纯内网部署和云服务器+安全加固两种部署方案。

**技术栈：**
- 前端：Vue 3 + TypeScript + Element Plus + ECharts
- 后端：Python 3.11+ / FastAPI
- 数据库：MySQL 8.0（结构化数据）+ ChromaDB/Milvus（向量检索）+ Redis（缓存/会话）
- LLM：支持 Ollama（本地）/ vLLM（高并发）/ OpenAI / 通义千问 / 文心一言

---

## 二、项目结构

```
Test/
├── backend/                          # 后端服务
│   ├── app/
│   │   ├── core/                     # 核心基础设施
│   │   │   ├── config.py             # 全局配置（环境变量）
│   │   │   ├── database.py           # SQLAlchemy 异步数据库
│   │   │   ├── redis.py              # Redis 连接池
│   │   │   └── errors.py             # 统一错误响应
│   │   ├── models/                   # 数据库模型（19张表）
│   │   ├── schemas/                  # 请求/响应数据结构
│   │   ├── services/                 # 业务服务层
│   │   │   ├── auth/                 # 认证服务
│   │   │   ├── permission/           # RBAC 权限服务
│   │   │   ├── security/             # 数据安全服务
│   │   │   ├── llm/                  # LLM 服务层
│   │   │   │   ├── adapters/         # 5种模型适配器
│   │   │   │   ├── tools/            # Agent 工具系统（24个工具）
│   │   │   │   ├── gateway.py        # LLM 统一网关
│   │   │   │   ├── conversation.py   # 对话管理器
│   │   │   │   ├── memory.py         # 长期记忆系统
│   │   │   │   ├── prompt_engine.py  # Prompt 模板引擎
│   │   │   │   ├── token_monitor.py  # Token 用量监控
│   │   │   │   └── security.py       # LLM 安全过滤
│   │   │   ├── rag/                  # RAG 知识检索
│   │   │   │   ├── engine.py         # 增强版检索引擎
│   │   │   │   ├── vector_store.py   # 向量存储层
│   │   │   │   └── query_enhancer.py # 查询增强器
│   │   │   └── report/               # 报表服务
│   │   ├── api/                      # API 路由（12个模块）
│   │   └── main.py                   # 应用入口
│   ├── alembic/                      # 数据库迁移
│   └── requirements.txt              # Python 依赖
│
├── frontend/                         # 前端应用
│   ├── src/
│   │   ├── router/                   # 路由配置
│   │   ├── stores/                   # Pinia 状态管理
│   │   ├── utils/api.ts              # HTTP 客户端
│   │   ├── layouts/                  # 页面布局
│   │   └── views/                    # 页面组件（10个）
│   └── package.json
│
└── 项目说明书.md                      # 本文件
```

---

## 三、运行方式

### 3.1 环境准备

**必需服务：**
- Python 3.11+
- Node.js 18+
- MySQL 8.0
- Redis 7.x
- ChromaDB（向量数据库）

**可选服务（根据 LLM 部署方案）：**
- Ollama（本地模型推理）
- vLLM（高并发推理）

### 3.2 后端启动

```bash
# 1. 进入后端目录
cd backend

# 2. 创建虚拟环境
python -m venv venv
venv\Scripts\activate     # Windows
# source venv/bin/activate  # Linux/Mac

# 3. 安装依赖
pip install -r requirements.txt

# 4. 配置环境变量（创建 .env 文件）
# 参考下方 [环境变量配置]

# 5. 初始化数据库
alembic upgrade head

# 6. 启动服务
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

### 3.3 前端启动

```bash
# 1. 进入前端目录
cd frontend

# 2. 安装依赖
npm install

# 3. 启动开发服务器
npm run dev
# 访问 http://localhost:3000
```

### 3.4 环境变量配置

在 `backend/` 目录创建 `.env` 文件：

```env
# 数据库
DATABASE_URL=mysql+aiomysql://root:password@localhost:3306/data_analysis

# Redis
REDIS_URL=redis://localhost:6379/0

# JWT 密钥（生产环境请使用强随机字符串）
JWT_SECRET_KEY=your-secret-key-change-in-production

# AES 加密密钥（64位十六进制，即32字节）
AES_SECRET_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# ChromaDB
CHROMADB_HOST=localhost
CHROMADB_PORT=8000

# Embedding 模型
EMBEDDING_MODEL_NAME=shibing624/text2vec-base-chinese

# 调试模式
DEBUG=true
```

### 3.5 启动 ChromaDB

```bash
# 使用 Docker
docker run -p 8000:8000 chromadb/chroma

# 或 pip 安装后直接运行
pip install chromadb
chroma run --port 8000
```

---

## 四、功能说明

### 4.1 智能问答（核心功能）

基于 RAG + LLM + 长期记忆的智能问答系统。

| 能力 | 说明 |
|------|------|
| 多轮对话 | 支持 20 轮上下文窗口，超出自动摘要压缩 |
| 流式回答 | SSE 实时逐字输出，体验流畅 |
| 长期记忆 | 自动记住历史问答，下次提问时召回相关记忆 |
| 工具调用 | 自动识别计算/查询需求，调用 24 个内置工具 |
| LLM 降级 | 模型不可用时自动展示原始检索文档 |
| 重新生成 | 对回答不满意可一键重新生成 |

### 4.2 知识检索（RAG）

增强型混合检索引擎，确保高召回率和准确性。

| 能力 | 说明 |
|------|------|
| 混合检索 | 向量语义检索 + 关键词精确匹配，双路召回 |
| 查询改写 | 自动去除停用词、提取关键词、同义词扩展 |
| 查询增强 | 上下文感知改写、复杂查询分解、HyDE 策略 |
| 智能切分 | 文档按段落/句子边界切分，保留语义完整性 |
| 重排序 | 综合向量分(50%) + 关键词命中(30%) + 来源质量(10%) |
| 结果去重 | Jaccard trigram 相似度去除重复片段 |
| 权限过滤 | 确保用户只能看到有权限的文档 |
| 结果缓存 | 相同查询 5 分钟缓存，减少重复计算 |

### 4.3 数据报表

| 能力 | 说明 |
|------|------|
| 可视化图表 | 表格、折线图、柱状图、饼图（ECharts） |
| 数据权限 | 不同角色看到不同维度和指标 |
| 分页加载 | 大数据量自动分页（>10万条） |
| 报表导出 | 支持 Excel 和 PDF 格式 |
| 异步导出 | 大报表异步生成，轮询状态后下载 |

### 4.4 Agent 工具系统

LLM 可自动调用的 24 个内置工具：

| 分类 | 工具 |
|------|------|
| 数学(9个) | 计算器、求和、平均值、中位数、标准差、百分位、变化率、复利、线性回归 |
| 地理(5个) | 坐标距离、城市坐标、城市间距离、坐标转换、方位角 |
| 时间(6个) | 当前时间、日期差、日期加减、工作日判断、工作日计数、时间戳转换 |
| 文本(4个) | 字数统计、JSON格式化、哈希计算、单位转换 |

**智能调用策略：**
- 意图匹配器：根据关键词和正则规则精准匹配相关工具
- 预执行机制：高置信度场景直接执行工具，结果作为事实注入 Prompt
- 后执行兜底：LLM 回答中的工具调用自动解析执行

### 4.5 长期记忆系统

| 能力 | 说明 |
|------|------|
| 自动存储 | 每次有价值的问答自动存入向量记忆库 |
| 语义召回 | 新问题自动检索相关历史记忆（top 3） |
| 去重判断 | 相似度 > 95% 的重复问题不重复存储 |
| 记忆衰减 | 90 天未被引用的记忆自动降低权重 |
| 容量管理 | 每用户最多 500 条记忆，满时淘汰低价值记忆 |
| 用户管理 | 可查看、搜索、删除个人记忆 |

### 4.6 安全体系

| 能力 | 说明 |
|------|------|
| RBAC 权限 | 基于角色的访问控制，最高权限优先合并 |
| AES-256 加密 | 敏感数据字段透明加解密 |
| Prompt 注入检测 | 10+ 正则模式识别恶意输入 |
| PII 脱敏 | 发送云端 LLM 前自动移除手机号/身份证等 |
| 频率限制 | 单用户 100 次/小时 LLM 调用上限 |
| 审计日志 | 所有操作完整记录（用户/时间/类型/范围） |
| 异常检测 | Redis 滑动窗口检测异常访问模式 |
| 会话安全 | JWT + 30分钟无操作超时 + 5次失败锁定15分钟 |

### 4.7 Token 用量监控

| 能力 | 说明 |
|------|------|
| 用量记录 | 每次 LLM 调用记录输入/输出 Token 数 |
| 费用估算 | 内置各模型定价表自动计算费用 |
| 配额管理 | 按用户/部门设置月度 Token 上限 |
| 预警机制 | 达到 80% 预警，100% 自动阻止 |
| 仪表盘 | 实时展示用量趋势、费用累计、排行榜 |

---

## 五、操作流程

### 5.1 管理员初始化流程

```
1. 配置环境变量 → 启动服务
2. 创建管理员账号（通过数据库或初始化脚本）
3. 创建部门和角色体系
   → 系统管理 → 角色权限 → 创建角色 → 配置权限
4. 配置 LLM 模型
   → 系统管理 → 模型管理 → 添加模型（填写 endpoint/API Key/优先级）
   → 执行健康检查确认模型可用
5. 上传知识库文档
   → 知识检索 → 调用 POST /api/v1/knowledge/documents
   → 系统自动切分、嵌入、建索引
6. 配置 Prompt 模板（可选）
   → 系统管理 → Prompt模板 → 创建分类模板
7. 设置 Token 配额
   → 系统管理 → Token监控 → 设置部门/用户配额
```

### 5.2 普通用户使用流程

```
1. 登录系统
   → 输入用户名/密码 → 获取 Token → 进入主界面

2. 智能问答
   → 侧边栏点击"智能问答"
   → 点击"新建对话"创建会话
   → 输入问题 → 系统自动：
       ① 检索知识库（混合检索 + 重排序）
       ② 检索长期记忆
       ③ 识别是否需要工具调用
       ④ 构建 Prompt（文档 + 记忆 + 工具 + 历史）
       ⑤ 调用 LLM 生成回答（流式输出）
       ⑥ 保存记忆
   → 查看回答（含引用来源）
   → 可点击"重新生成"获取新回答

3. 知识检索
   → 侧边栏点击"知识检索"
   → 输入查询 → 可选是否启用 LLM 回答
   → 查看检索结果和 AI 回答
   → 展开查看原始文档来源

4. 数据报表
   → 侧边栏点击"数据报表"
   → 选择报表类型
   → 设置时间范围和筛选条件
   → 查看可视化图表和数据表格
   → 可导出为 Excel/PDF

5. 我的记忆
   → 侧边栏点击"我的记忆"
   → 查看系统自动保存的历史问答记忆
   → 可按主题筛选、语义搜索
   → 可删除不需要的记忆
```

### 5.3 API 调用流程（开发者参考）

**认证流程：**
```
POST /api/v1/auth/login {"username": "xxx", "password": "xxx"}
  → 返回 access_token
后续请求 Header: Authorization: Bearer {access_token}
```

**知识检索流程：**
```
POST /api/v1/knowledge/search
  {"query": "Q3销售数据环比", "top_k": 5, "use_llm": true}
  → 返回 answer + sources（含相关度评分）
```

**对话流程：**
```
POST /api/v1/llm/conversations          # 创建会话
POST /api/v1/llm/conversations/{id}/messages  # 发送消息（SSE 流式）
GET  /api/v1/llm/conversations/{id}/messages  # 获取历史
```

**文档管理流程：**
```
POST   /api/v1/knowledge/documents      # 上传文档
DELETE /api/v1/knowledge/documents/{id}  # 删除文档
GET    /api/v1/knowledge/stats          # 知识库统计
```

---

## 六、API 端点汇总

| 模块 | 端点 | 说明 |
|------|------|------|
| 认证 | POST /auth/login | 登录 |
| | POST /auth/logout | 登出 |
| | GET /auth/me | 当前用户信息 |
| 角色 | GET/POST/PUT/DELETE /roles | 角色 CRUD |
| 权限 | POST /users/{id}/roles | 分配角色 |
| | GET /users/{id}/effective-permissions | 有效权限 |
| 知识 | POST /knowledge/search | 知识检索 |
| | POST/DELETE /knowledge/documents | 文档管理 |
| 报表 | GET /reports | 报表列表 |
| | POST /reports/{id}/generate | 生成报表 |
| | POST /reports/{id}/export | 导出报表 |
| 对话 | POST /llm/conversations | 创建会话 |
| | POST /llm/conversations/{id}/messages | 发送消息 |
| | POST /llm/conversations/{id}/regenerate | 重新生成 |
| 模型 | GET/POST/DELETE /llm/models | 模型管理 |
| | POST /llm/models/{id}/health-check | 健康检查 |
| 模板 | GET/POST/PUT /prompts/templates | Prompt 模板 |
| | POST /prompts/templates/{id}/preview | 预览 |
| | POST /prompts/templates/{id}/rollback/{v} | 版本回退 |
| Token | GET /tokens/usage | 用量统计 |
| | GET /tokens/dashboard | 仪表盘 |
| | POST /tokens/quotas | 设置配额 |
| 记忆 | GET /memories | 记忆列表 |
| | POST /memories/search | 语义搜索记忆 |
| | DELETE /memories/{id} | 删除记忆 |
| 工具 | GET /tools | 工具列表 |
| | POST /tools/execute | 执行工具 |
| 审计 | GET /audit/logs | 审计日志 |

（所有端点前缀为 `/api/v1`）

---

## 七、部署方案

### 方案 A：纯内网部署

适用于数据安全要求最高的场景。

- 所有服务部署在企业内网，无外网连接
- LLM 使用 Ollama/vLLM 本地推理
- Embedding 模型本地部署
- 知识库通过安全介质离线导入
- 依赖包预装（pip download 打包）

### 方案 B：云服务器 + 安全加固

适用于需要弹性扩容的场景。

- VPC 网络隔离 + VPN 接入
- LLM 可对接云端 API（OpenAI/通义千问/文心一言）
- API Key 通过 KMS 加密存储
- IP 白名单 + 防火墙 + DDoS 防护
- 数据子网与应用子网分离
- 出站规则仅允许指定 LLM API 端点

---

## 八、注意事项

1. **首次运行**需要执行数据库迁移（`alembic upgrade head`）创建所有表
2. **Embedding 模型**首次加载会自动下载（约 400MB），内网环境需提前准备模型文件
3. **LLM 模型**必须至少配置一个可用模型才能使用智能问答功能
4. **Token 配额**未设置时不限制调用次数
5. **知识库**为空时，智能问答仍可基于对话历史和记忆工作，但无法提供基于文档的回答
6. **生产环境**务必修改 JWT_SECRET_KEY 和 AES_SECRET_KEY 为强随机值
7. **Redis** 用于会话管理和缓存，服务重启后会话失效需重新登录
